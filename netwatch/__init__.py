"""netwatch: a web console for full network monitoring.

Tracks IP host wake/sleep transitions (active ICMP polling) alongside the
MAC/BSSID connection records captured by rf_sniffer (passive WiFi + BLE),
and serves both through one filterable web dashboard.
"""

__version__ = "0.1.0"
