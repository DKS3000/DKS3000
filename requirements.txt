# Core analyzer (export-based) needs only the standard library.
# Required only for `instagram_analyzer fetch` (live Graph API mode):
requests>=2.31

# Required only for rf_sniffer's `wifi`/`both` commands (raw 802.11 capture):
scapy>=2.5

# Required only for rf_sniffer's `ble`/`both` commands (BLE scanning):
bleak>=0.21

# Required only for running the test suite:
pytest>=7.4

# Required for kali_dashboard.py (all optional per-feature, see its docstring):
pyshark>=0.6      # live/offline packet capture (needs tshark from apt)
selenium>=4.0     # UI automation testing (needs chromium-driver from apt)
paramiko>=3.0     # SSH router log retrieval
